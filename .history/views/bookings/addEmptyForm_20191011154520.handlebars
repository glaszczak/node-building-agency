<div class="card card-body">
    <h3>
        New Booking
    </h3>
    <form action="/bookings/add/{{buildings.idBuildings}}" method="post">
        <div class="form-field mb-2">
            <label for="buildingFilter">
                Building
            </label>
            <select id="buildingFilter" class="form-control" name="selectBuilding" required>
                <option>
                    Choose building
                </option>
                {{#each buildings}}
                <option id={{this.idBuildings}}>
                    {{this.address}}
                </option>
                {{/each}}
            </select>
        </div>
        <div class="form-group mb-2">
            <label for="startDate">
                Start date
            </label>
            <input type="date" class="form-control" name="startDate" required />
        </div>
        <div class="form-group mb-3">
            <label for="endDate">
                End date
            </label>
            <input type="date" class="form-control" name="endDate" required />
        </div>
        <button class="btn btn-primary">
            Get Available Contractors
        </button>
    </form>
</div>